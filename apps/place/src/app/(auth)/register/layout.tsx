import { PropsWithChildren } from "react";

export const metadata = {
  title: "가입 | Flick Place",
};

export default function RegisterLayout({ children }: PropsWithChildren) {
  return <>{children}</>;
}
