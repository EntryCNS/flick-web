export const metadata = {
  title: "공지사항 | Flick Admin",
  description: "Flick Admin 공지사항 페이지",
};


export default function ChargeHistoryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}