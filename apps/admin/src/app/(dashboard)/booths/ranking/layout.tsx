export const metadata = {
    title: "부스 랭킹 | Flick Admin",
    description: "Flick Admin 부스 랭킹 페이지",
  };
  
  
  export default function BoothApprovalLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <>{children}</>;
  }